# For running locally on your own PC
[local]
localhost ansible_connection=local

# For running on a remote server (optional)
# [remote]
# 192.168.1.100 ansible_user=ubuntu ansible_ssh_private_key_file=~/.ssh/id_rsa